@using ShwasherSys
@using ShwasherSys.Models.Modal
@model string
@{
    string originFiled = "no,name",
        //targetFiled = string.IsNullOrEmpty(Model) ? originFiled : Model;
        targetFiled = Model ?? "";
    List<QueryItem> queryItems = new List<QueryItem>()
{

    new QueryItem("no", "资产编号",true),
    new QueryItem("name", "资产名称",true),
};


    QueryModalModel qmModel = new QueryModalModel("设备信息", "/api/services/app/Query/QueryFixedAsset", queryItems, originFiled, targetFiled, modalId: "query_device_modal");
}
@Html.Partial("Modals/Query/_QueryModal", qmModel)



